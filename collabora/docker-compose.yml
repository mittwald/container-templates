services:
  collabora:
    image: collabora/code:latest
    ports:
      - "9980:9980"
    environment:
      - TZ=Europe/Berlin
      - extra_params=--o:ssl.enable=false --o:ssl.termination=true --o:net.post_allow.host[0]=${WOPI_HOST} --o:storage.wopi.host[0]=${WOPI_HOST} --o:server_name=${HOST}
