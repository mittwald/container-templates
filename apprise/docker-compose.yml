services:
  apprise:
    image: caronc/apprise:latest
    environment:
      - TZ=Europe/Berlin
      - APPRISE_STATEFUL_MODE=simple
      - APPRISE_WORKER_COUNT=1
      - APPRISE_ADMIN=yes
    volumes:
      - apprise_config:/config
volumes:
  apprise_config:
