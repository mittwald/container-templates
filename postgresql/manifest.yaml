manifestVersion: 1.0
name:
  de: PostgreSQL
  en: PostgreSQL
version: "18.x.x"
type: component
tagline:
  de: Leistungsstarke relationale Open-Source Datenbank
  en: Powerful open-source relational database
description:
  de: PostgreSQL ist eine leistungsstarke, objektrelationale Open-Source Datenbank mit über 35 Jahren aktiver Entwicklung. Ideal als Backend für Webanwendungen, Analyseplattformen und geschäftskritische Systeme.
  en: PostgreSQL is a powerful open-source object-relational database with over 35 years of active development. Ideal as a backend for web applications, analytics platforms and business-critical systems.
developer: PostgreSQL Global Development Group
website: https://www.postgresql.org
repository: https://github.com/postgres/postgres
license:
  name: PostgreSQL License
  link: https://spdx.org/licenses/PostgreSQL.html
categories:
  - development
userInputs:
  - name: "DATABASE_NAME"
    positionMeta: { step: "common", index: 1 }
    required: true
    validationSchema: '{ "type": "string", "minLength": 1 }'
  - name: "ADMIN_USER"
    positionMeta: { step: "common", index: 2 }
    required: true
    validationSchema: '{ "type": "string", "minLength": 1 }'
  - name: "ADMIN_PASSWORD"
    positionMeta: { step: "common", index: 3 }
    required: true
    validationSchema: '{ "type": "string", "minLength": 12, "format": "password" }'

help:
  technicalDetails:
  - key:
      de: Hostname
      en: Hostname
    value: ${postgres.hostname}
  - key:
      de: Port
      en: Port
    value: 5432/TCP
  - key:
      de: Datenbank-Name
      en: Database Name
    value: ${postgres.env.POSTGRES_DB}
  - key:
      de: Admin Benutzername
      en: Admin username
    value: ${postgres.env.POSTGRES_USER}
  - key:
      de: Admin Passwort
      en: Admin Password
    value: ${postgres.env.POSTGRES_PASSWORD}
  - key:
      de: Verbindungsstring
      en: Connection string
    value: postgresql://${postgres.env.POSTGRES_USER}:${postgres.env.POSTGRES_PASSWORD}@${postgres.hostname}/${postgres.env.POSTGRES_DB}
