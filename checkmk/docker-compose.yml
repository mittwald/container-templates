services:
  checkmk:
    image: checkmk/check-mk-community:latest
    ports:
      - "5000:5000"
    environment:
      - TZ=Europe/Berlin
      - CMK_SITE_ID=cmk
      - CMK_PASSWORD=${ADMIN_PASSWORD}
    volumes:
      - checkmk_sites:/omd/sites
volumes:
  checkmk_sites:
