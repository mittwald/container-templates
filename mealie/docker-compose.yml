services:
  mealie:
    image: ghcr.io/mealie-recipes/mealie:latest
    ports:
      - "9000:9000"
    environment:
      - TZ=Europe/Berlin
      - BASE_URL=https://${HOST}
      - ALLOW_SIGNUP=false
    volumes:
      - mealie_data:/app/data
volumes:
  mealie_data:
